# Flood Inundation — Decision Log

Append-only. Each row records a fork resolved by the lead loop (6-axis rubric in `Skills/skills/flood-inundation-lead/SKILL.md`).

| ID | Date | Fork | Options considered | Choice | Rationale | Revisit when |
|----|------|------|-------------------|--------|-----------|--------------|
| D-001 | 2026-06-17 | HAND variant for confluences | plain HAND; GMS/level-path HAND; full 2D solver | **GMS/level-path HAND** | Defensibility 4/5, global 5/5, UX 4/5 (fast scrubber), build 4/5 (pyflwdir in stack), vision 4/5, demand 5/5 (NOAA OWP operational). Plain HAND fails at junctions. 2D fails build-cost veto for Phase 1. | Junction-heavy urban networks still show false negatives after GMS |
| D-002 | 2026-06-17 | pyflwdir `hand()` API | manual HAND raster; pyflwdir.hand(drain, elev); external NOAA FIM repo | **pyflwdir.hand(drain, elev)** | Reuses existing dep; drain = acc≥threshold stream mask; elev = conditioned DEM. Documented in `library_refs/pyflwdir.json`. | pyflwdir HAND disagrees with NOAA FIM on reference hindcast |
| D-003 | 2026-06-17 | Stage→extent animation storage | N full depth GeoTIFFs; vector extent polygons + one raster/frame; precomputed COG pyramid only | **Vector extent + one raster/frame** | UX 5/5 (smooth scrub), build 4/5, integrates with existing time slider. N rasters janky on map push. | Frame count >500 per event |
| D-004 | 2026-06-17 | Reference demo basin | global GLO-30 pour point; CONUS `01031500`; user-picked | **CONUS `01031500` default** | 3DEP + NWIS + GFM validation path; agent may override. | User names region |
| D-005 | 2026-06-17 | Phase 1 validation transport | full GFM fetch in tool; mask-only metrics module; defer validation | **mask-only metrics module + optional GeoJSON ref on tool** | Bench/oracle needs deterministic CSI without network; GFM fetch deferred to Phase 2 live hook. Tool accepts reference GeoJSON for hindcast when available. | GFM MCP product lands in aihydro-data |
| D-006 | 2026-06-17 | Phase 1 population exposure | defer; WorldPop GEE fetch; density placeholder | **density placeholder × inundated area** | Ships exposure narrative in Phase 1 with explicit licence note; avoids blocking on GEE auth. | WorldPop product in aihydro-data |
| D-007 | 2026-06-17 | Stage scrubber UX | MCP re-call per stage; metadata-only scrubber; defer UI | **metadata-only scrubber in map ribbon** | Reads `stage_lookup_json`; full re-map needs MCP (Phase 2 hydrograph animation). Zero backend risk. | Hydrograph time slider wired to per-timestep extents |
| D-008 | 2026-06-17 | Live GFM transport | GEE asset; EODC STAC COG; fixture only | **EODC STAC + ensemble_flood_extent COG** | Auth-free HTTPS; empty flood in AOI is a valid live result; fixture on network error. | GFM portal REST with user token |
| D-009 | 2026-06-17 | Phase 3 physics when SFINCS absent | fail job; skip benchmark; morphological proxy | **morphological proxy + explicit proxy_note** | Validate-tier job must complete offline in CI; proxy labeled, never operational physics. | Automated session→SFINCS mesh lands |
| D-010 | 2026-06-17 | Phase 4 3D on 2D map | Cognaterra-only; deck.gl mesh on existing MapView | **deck.gl SimpleMeshLayer + optional pitch toggle** | Keeps 2D canonical; 3D is presentation-only; Cognaterra terrain deferred. | Cognaterra MapLibre terrain embed |
| D-011 | 2026-06-17 | Phase 4 terrain context | embed Cognaterra; skip terrain | **deck.gl Terrarium TerrainLayer (toggle)** | Same Terrarium source as Cognaterra; no MapLibre rewrite; vertical datum caveat documented. | Cognaterra interleaved map |
| D-012 | 2026-06-17 | Phase 4 cinematic camera | Cognaterra flyTo only; static 3D | **manifest camera_path + MapView playback** | Ships hydrograph flythrough in existing map; AOI-axis traverse is presentation-only. | Flowline-following path from MERIT |
| D-013 | 2026-06-17 | Camera path geometry | MERIT rivers GeoJSON; bounds axis; D8 stem | **D8 main-stem trace from HAND stack fdir/acc** | Reuses same conditioning as HAND; no extra fetch; MERIT centerline deferred. | MERIT river polyline in manifest |
| D-014 | 2026-06-17 | Camera path when MERIT present | MERIT flowline only; D8 only; bounds only | **MERIT flowline when `flowline_geojson` in stack, else D8 stem, else bounds axis** | Follows visible river network when local MERIT vectors are installed; D8 fallback offline-safe. | MERIT fetch fails on reference basin |
| D-015 | 2026-06-17 | 3D vertical alignment | ignore; user slider only; EGM96-lite offset in manifest | **EGM96-lite geoid offset in manifest; client applies when terrain on** | Closes ~30 m CONUS gap between NAVD88 HAND mesh and Terrarium without network fetch; labeled presentation-only. | Full EGM96 grid or per-vertex sampling |
| D-016 | 2026-06-17 | Phase 3 surrogate substrate | defer; full SWE-GNN trainer; RLE JSON export from physics jobs | **RLE JSON export + validation_masks.npz; train tool deferred** | Ships offline-safe dataset contract (B-078) without torch/graph deps; pairs with future train job. | Multi-basin fleet + SFINCS depth targets |
| D-017 | 2026-06-17 | Phase 3 surrogate trainer | defer; SWE-GNN torch; sklearn area regression | **Morphology dilation baseline job (numpy grid search)** | Trains from B-078 JSON without new deps; explicit caveat; CSI gain on synthetic proxy pairs. | SWE-GNN graph model + multi-basin fleet |
| D-018 | 2026-06-25 | Flood data/source ownership | direct py3dep/GFM clients in tools; watershed-owned DEM policy; `aihydro-data` product policy | **Route production data choices through `aihydro-data`; keep HAND DEM spike only as offline benchmark** | Ecosystem layering: `aihydro-data` owns fetch policy, provenance, source fallback, citations. Web/catalog checks confirmed EODC STAC exposes GFM (`https://stac.eodc.eu/api/v1/collections/GFM`); DEM policy belongs in `aihydro-data` where GLO-30/3DEP/MERIT fallbacks can be evaluated and cited centrally. | A new hydrologic source is considered or `aihydro-data` product registry changes |
