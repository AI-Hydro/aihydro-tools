{
  "library": "xarray",
  "version_tested": "2024.x",
  "purpose": "N-dimensional labeled arrays \u2014 used for gridded climate/DEM data",
  "install": "pip install xarray",
  "field_mappings": {
    "Dataset": {
      "note": "Collection of DataArrays sharing coordinates. Like a dict of arrays.",
      "access_variable": "ds['precip'] or ds.precip",
      "dims": "Named dimensions, e.g. ('time', 'lat', 'lon')"
    },
    "DataArray": {
      "note": "Single labeled array with coordinates.",
      "to_pandas": ".to_pandas() \u2192 pd.Series (1D) or pd.DataFrame (2D)",
      "sel": ".sel(time='2000-01-01') or .sel(lat=slice(40,45))",
      "isel": ".isel(time=0) for integer indexing"
    }
  },
  "gotchas": [
    "xarray coordinates may be named 'lat'/'lon' or 'y'/'x' or 'latitude'/'longitude' depending on source. Always check ds.coords.",
    "Spatial mean over a polygon: use .where(mask).mean(dim=['lat','lon']) \u2014 not just .mean().",
    "Date selection: .sel(time='2000') selects ALL of year 2000. .sel(time=slice('2000','2001')) is a range.",
    "GridMET output uses 'lat' and 'lon' dims. py3dep output uses 'y' and 'x'.",
    "xarray lazy loads from NetCDF \u2014 call .load() or .compute() to force evaluation before looping.",
    "Avoid calling .values on large DataArrays inside loops \u2014 vectorize operations instead.",
    "Time coordinates may be cftime objects (not numpy datetime64) for pre-1678 data. Use xr.cftime_range."
  ],
  "common_patterns": {
    "spatial_mean_over_time": "# ds has dims (time, lat, lon)\nbasin_mean = ds['pr'].mean(dim=['lat', 'lon'])  # \u2192 DataArray(time)\ndf = basin_mean.to_pandas()  # \u2192 pd.Series",
    "select_time_range": "subset = ds.sel(time=slice('2000-01-01', '2010-12-31'))",
    "resample_to_monthly": "monthly = ds['pr'].resample(time='MS').sum()"
  },
  "version_compatible": ">=0.19.0,<2025.0"
}