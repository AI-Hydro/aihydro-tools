{
  "library": "pygridmet",
  "version_tested": "0.16.x",
  "purpose": "Access GridMET daily gridded climate data (1979-present) over CONUS",
  "install": "pip install pygridmet",
  "field_mappings": {
    "GridMET.get_bygeom": {
      "variables_arg": "list of variable names (strings)",
      "variable_names": {
        "pr": "precipitation (mm/day)",
        "tmmx": "maximum temperature (K) \u2014 subtract 273.15 for \u00b0C",
        "tmmn": "minimum temperature (K) \u2014 subtract 273.15 for \u00b0C",
        "pet": "reference ET (mm/day)",
        "srad": "downward shortwave radiation (W/m\u00b2)",
        "vs": "wind speed at 10m (m/s)",
        "rmax": "relative humidity max (%)",
        "rmin": "relative humidity min (%)"
      },
      "output_type": "xarray Dataset with lat/lon spatial grid",
      "note": "Returns xr.Dataset. Extract basin mean: ds['pr'].mean(dim=['lat','lon'])"
    }
  },
  "gotchas": [
    "GridMET only covers CONUS (contiguous US). Fails for Alaska, Hawaii, Puerto Rico.",
    "Temperature variables (tmmx, tmmn) are in Kelvin \u2014 always subtract 273.15.",
    "get_bygeom() requires geometry in EPSG:4326 (WGS84).",
    "Spatial averaging over a polygon: use .where(mask).mean() with a rasterized mask.",
    "Date range format: ('YYYY-MM-DD', 'YYYY-MM-DD') as tuple.",
    "Large date ranges (>10 years) over large watersheds may be slow \u2014 consider chunking by year."
  ],
  "common_patterns": {
    "fetch_precip_temp": "from pygridmet import GridMET\nimport geopandas as gpd\ngdf = gpd.read_file('watershed.geojson')\nds = GridMET('pr,tmmx,tmmn', dates=('2000-01-01', '2020-12-31')).get_bygeom(gdf.geometry.iloc[0])\npr = ds['pr'].mean(dim=['lat','lon']).to_pandas()\ntmax_c = ds['tmmx'].mean(dim=['lat','lon']).to_pandas() - 273.15"
  },
  "version_compatible": ">=0.14.0,<0.18.0"
}